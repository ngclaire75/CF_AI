# CF_AI — Core dependencies (required)

# AI providers
openai>=1.30.0
anthropic>=0.40.0

# Model Context Protocol server (for WordPress MCP tools)
mcp>=1.0.0

# Dashboard — Flask (legacy, keep for reference)
flask>=3.0.0

# Dashboard — FastAPI + Starlette + Pydantic (primary API server)
# Starlette: async web framework (routing, middleware, WebSocket, SSE, background tasks)
# Pydantic: request/response validation with type-checked models
# uvicorn: ASGI server to run FastAPI
fastapi>=0.115.0
starlette>=0.40.0
pydantic>=2.7.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9     # required for form data / file uploads in FastAPI
websockets>=13.0             # WebSocket support (uvicorn[standard] also installs this)
httpx>=0.27.0                # async HTTP client — used in background tasks

# .env file loading
python-dotenv>=1.0.0

# Jinja2 for FastAPI templates (also used by Flask)
jinja2>=3.1.0

# Cloudflare/WAF bypass
cloudscraper>=1.2.71         # solves CF JS challenge v1/v2 — used in wordpress_mcp.py
requests>=2.31.0             # required by cloudscraper

# Security scanning frameworks
python-nmap>=0.7.1           # nmap port scanner Python binding
wappalyzer>=0.4.0            # technology fingerprinting
bs4>=0.0.2                   # BeautifulSoup — HTML parsing for crawlers
